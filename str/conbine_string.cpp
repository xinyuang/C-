#include <string>
#include <iosream>
using namespace std;

void combine(string a, string b) {
	string b_copy = str.copy(b);
	b.erase(0);
	cout << "b= " << b << endl;
	cout << "b_c = " << c_copy;
}

int main()
{
	string a = 'abcdef';
	string b = 'fedh';
	conbine(a, b);
}